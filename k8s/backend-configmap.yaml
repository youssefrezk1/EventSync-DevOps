apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: eventsync
data:
  PORT: "4000"
  JWT_EXPIRES_IN: "7d"
  WORKSHOP_FIXED_PRICE: "100"
  FRONTEND_URL: "http://temporary.eventsync.local"
